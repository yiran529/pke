# 多核内存管理改进：加锁保护物理页分配

- 在 `kernel/pmm.c` 引入自旋锁 `g_pmm_lock`，用 `pmm_lock/pmm_unlock` 包裹 `alloc_page` 与 `free_page`，保护空闲页链表的并发访问，避免多核同时操作导致链表损坏、分配返回物理地址 0。

# 多进程独立虚拟地址游标：每进程 ufree_page

- 在 `process` 结构新增 `ufree_page` 字段；在 `load_user_program` 初始化为 `USER_FREE_ADDRESS_START`，在 `sys_user_allocate_page` 递增并映射，取代全局 `g_ufree_page`。这样各核/进程拥有独立的用户虚拟地址分配游标，保证虚拟地址连续且互不干扰。

# 神金错误
忘记在kernel初始化时将 `trapframe->regs.tp = hartid`
导致严重问题的可能原因：影响了`read_tp`等操作

# 一些总结

### RISC-V 特权级切换核心机制深度总结

#### 1. 为什么要将 `trapframe` 映射到用户页表？

* **桥梁作用**：执行 `ecall` 后，硬件虽进入 S 态，但**页表寄存器 `satp` 尚未切换**。此时 CPU 仍处于用户页表的“视野”中。
* **寻址前提**：内核必须立刻保存寄存器。如果不做映射，`trap_entry` 的第一行保存指令就会因为在当前页表中找不到 `trapframe` 的虚拟地址而触发 **Page Fault**，导致内核在处理异常前就先崩溃。
* **权限控制**：映射时设置 PTE 的 `U=0`，确保用户态程序无法访问，只有进入 S 态后内核才能合法读写。

#### 2. 为什么不能一进入 S 态就换页表？

* **无中转寄存器**：修改 `satp` 必须通过通用寄存器（如 `t0`）。但此刻所有通用寄存器（x1-x31）都装满了用户数据，直接覆盖任何一个都会导致用户现场永久损坏。
* **中转逻辑细节**：硬件不支持“内存直达 `satp`”。必须执行 `ld t0, (address); csrw satp, t0`。在这个过程中，**你必须先有一个寄存器来执行 `ld` 操作**，而这个寄存器的腾空必须依赖第一步的现场保存。
* **地址一致性**：切换 `satp` 的瞬间地址映射会剧变，必须保证切换指令前后的 PC 地址在两个页表中都是一致映射的（Trampoline），否则执行流会直接断裂。

#### 3. sscratch 的作用与 csrrw 交换的**底层硬件限制**

* **【核心限制】CSR 无法寻址**：这是最关键的一点。在 RISC-V 指令集中，**`sd/ld`（存取内存）指令的基地址寄存器必须是通用寄存器（GPR）**。硬件上没有设计将 CSR 寄存器（如 `sscratch`）直接连接到内存寻址总线的路径。
* **必须交换的原因**：
1. **寻址合法化**：你不能执行 `sd sp, 0(sscratch)`，所以必须用 `csrrw` 将指针从 `sscratch` 换到通用寄存器（如 `a0`）中，**使之成为合法的内存基地址**。
2. **原子暂存**：在拿到指针的同时，`csrrw` 利用硬件电路的特性，顺手将用户原本在 `a0` 里的数据“踢”回 `sscratch` 暂存。这样在完全没有空余寄存器的情况下，完成了“拿钥匙”和“保现场”的双重任务。



---

### 💡 总结：OS 设计的底层思想

这套机制深刻体现了 **硬件与软件的“极简协作” (Co-design)** 思想：

1. **硬件极简（Mechanisms）**：硬件不提供昂贵的“全自动”切换（如 x86 的硬件压栈），只提供最基础的 CSR 寄存器和一条原子交换指令 `csrrw`。这极大降低了 RISC-V 核心的流水线复杂度。
2. **软件策略（Policies）**：将具体的现场保存逻辑交给内核开发者。这种“手动挡”的设计赋予了 OS 极高的灵活性（例如可以根据进程类型选择保存多少寄存器）。
3. **自洽性要求**：在特权级切换这种“环境剧变”的瞬间，OS 必须通过精巧的地址映射重叠和寄存器原子操作，确保系统逻辑在任何一个时钟周期内都不会出现“无处可落脚”的崩溃。