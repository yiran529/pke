# 大致流程 maybe
实现 **Copy-on-Write (COW)** 的核心在于：**“延迟复制”和“利用异常处理机制”**。

在像 PKE 这样基于 RISC-V 的操作系统中，实现 COW 大致分为以下四个核心步骤：

---

### 1. `fork` 时的处理（共享阶段）

当父进程调用 `fork` 时，内核不再直接为子进程分配物理内存并拷贝内容，而是：

* **拷贝页表**：子进程获得一份和父进程一模一样的页表。
* **修改权限**：将父子进程页表中所有的 **PTE_W（可写）** 位清零，即全部设为**只读**。
* **标记 COW**：利用 PTE 中保留的位（RSW 位，软件自定义位）将这些页面标记为“这是由 COW 产生的只读页”。
* **增加引用计数**：在一个全局的物理页管理表中，将这些物理页的引用计数（Reference Count）加 1。

---

### 2. 触发缺页异常（异常捕获）

当父进程或子进程尝试修改某个页面时（例如执行 `sd` 指令）：

* **硬件报警**：由于页表权限是“只读”，硬件会触发 **Store Page Fault**（异常代码 15）。
* **进入内核**：CPU 跳转到 `trap_handler`。
* **识别逻辑**：内核检查触发异常的虚拟地址对应的 PTE。如果发现该页被标记为“COW 页”，则进入 COW 恢复逻辑；如果是真正的只读页，则报 Segment Fault。

---

### 3. 内存分裂（真正复制）

一旦确定是 COW 引起的异常，内核执行以下操作：

1. **检查引用计数**：
* 如果该物理页的引用计数 **> 1**：说明还有其他进程在共享它。内核分配一个**新的物理页**，将旧页内容 `memcpy` 过去。
* 如果引用计数 **= 1**：说明此时只有一个进程在用（其他进程可能已经退出了），无需复制，直接进入第 3 步。


2. **更新页表**：将当前进程的页表指向新的物理页。
3. **恢复权限**：将该 PTE 标记为 **可写 (PTE_W)**，并清除 COW 标记。
4. **递减计数**：将旧物理页的引用计数减 1。

---

### 4. 物理页释放（内存回收）

在进程退出（`exit`）或由于 COW 分裂旧页时：

* 每次释放一个页面，都要将引用计数减 1。
* **只有当引用计数减为 0 时**，才调用内存分配器（如 `kfree`）将该物理页放回空闲链表。

---

### 核心代码逻辑伪代码

```c
void handle_cow_fault(pagetable_t page_table, uint64 vaddr) {
    pte_t *pte = get_pte(page_table, vaddr);
    uint64 old_pa = PTE2PA(*pte);

    if (get_refcount(old_pa) > 1) {
        // 分配新页面
        uint64 new_pa = (uint64)kalloc();
        // 物理拷贝内容
        memcpy((void*)new_pa, (void*)old_pa, PGSIZE);
        // 更新页表：指向新物理页，并开启写权限
        *pte = PA2PTE(new_pa) | PTE_V | PTE_U | PTE_R | PTE_W;
        // 旧页面引用减少
        decrease_refcount(old_pa);
    } else {
        // 只有我一个人用，直接开写权限即可
        *pte |= PTE_W;
    }
    // 刷新 TLB，确保新的页表属性生效
    asm volatile("sfence.vma zero, %0" : : "r"(vaddr));
}

```

---

### 💡 实现 COW 的难点

1. **引用计数表 (Refcount Table)**：你需要一个数组来存储所有物理页的引用次数。通常可以用 `uint16_t ref_count[PHY_MEM_SIZE / PGSIZE]` 来实现。
2. **并发安全**：在多核环境下，增加和减少引用计数、修改页表必须是原子操作（记得之前的 `amoswap` 吗？这里可能需要 `amoadd` 或者锁保护）。
3. **栈与堆的区分**：虽然逻辑一致，但要确保内核栈（Kernel Stack）本身不要误设为 COW，否则内核一旦发生异常就再也回不来了。

**你现在的 PKE 实验已经有物理内存引用计数的功能了吗？这是实现 COW 最基础的“基建”。**

---

# 我需要构造一个全局数据结构记录每个物理页被引用的次数（用于实现COW），怎么设计是最佳实践
下面给出一个业内常见且稳妥的设计方案（含实现思路、并发/边界条件说明、以及示例代码片段），用于在内核中维护每个物理页的引用计数以支持 Copy-On-Write（COW）。我会说明为什么这样做是“最佳实践”，并给出可直接移植到你当前代码（`pmm.c` / `vmm.c`）的参考实现草案。你想让我直接把代码补到仓库里吗？我可以帮你实现并运行简单检查。

设计要点（摘要）
- 数据结构：为所有物理页分配一块连续的数组 `page_refcount[]`，每个条目为 32 位（或根据内存选择 16 位），存放对应物理页的引用计数。索引由物理地址转换： idx = (pa - DRAM_BASE) / PGSIZE。
- 存放位置：在 `pmm_init()` 创建空闲页链表之前，从 free region 的起始处预留出若干页用于保存 `page_refcount`（避免它被当作普通空闲页回收）。
- 并发：使用原子操作（GCC 的 `__sync_*` 或 C11 `atomic_*`）对引用计数进行增减，以保证多核/并发环境安全；在一些需要原子“减到 0 时释放页”的场景，使用原子 fetch-and-sub 或 CAS 来避免竞态和重复释放。
- 接口：提供 `page_ref_init()`, `page_ref_inc(pa)`, `page_ref_dec(pa)`, `page_ref_get(pa)` 等简单 API。`page_ref_dec` 可以选择返回新的计数或在计数变 0 时自动释放物理页（可选，需小心）。
- 内核保护：把为元数据（refcount 数组）保留的页面也标记不在普通分配池中，避免被 `alloc_page()` 分配到用户页上；并把这些元数据的 refcount 初始化为 1 或特殊值防止误释放。
- COW 工作流：fork 时对被共享的物理页调用 `page_ref_inc(pa)` 并把 PTE 中的写位清除（只保留读/执行）；写时缺页/写保护异常处理：检查 refcount，若 >1 则分配新页并复制内容，dec 旧页；若 ==1 则直接给页加写权限（修改 PTE）。

实现细节与示例代码（可贴入 `pmm.c` / 新建 `pmm_ref.c`）
下面给出的实现尽量贴合你当前代码风格与已有 API（`alloc_page()`, `free_page()`, `g_mem_size`, `DRAM_BASE`, `PGSIZE` 等）。注意：这是参考实现，集成时请根据项目编译选项、头文件包含路径微调（比如 `#include <stdatomic.h>` / 使用 `__sync_*` 内建函数）。

1) 在 `pmm.c`（或新建 `pmm_ref.c` + 声明到 `pmm.h`）添加这些全局变量：

```c
// 全局引用计数数组 & 元数据
static uint32_t *page_refcount = 0; // 指向 refcount 数组
static uint64_t nphys_pages = 0;    // 物理页总数
static uint64_t refcount_pages = 0; // 用于存放数组所占页数
```

2) 在 `pmm_init()` 中：在创建 freepage list (`create_freepage_list`) 之前预留元数据页并初始化数组：

策略：
- 计算 total_pages = (free_mem_end_addr - free_mem_start_addr) / PGSIZE。
- 计算数组字节数 = total_pages * sizeof(uint32_t)。
- 计算需要的页数 `refcount_pages = (bytes + PGSIZE-1)/PGSIZE`。
- 将 `page_refcount` 指向 `free_mem_start_addr`，然后把 `free_mem_start_addr += refcount_pages * PGSIZE`，再继续创建 free list（这样这些页不会被回收到 free list）。

示例片段（在 `pmm_init()` 的相应位置）：

```c
  // 1) 计算物理页总数（基于 DRAM_BASE 到 free_mem_end_addr）
  uint64_t total_bytes = free_mem_end_addr - free_mem_start_addr;
  nphys_pages = total_bytes / PGSIZE;

  // 2) 计算存放 refcount 数组需要的页数
  uint64_t bytes_for_ref = nphys_pages * sizeof(uint32_t);
  refcount_pages = (bytes_for_ref + PGSIZE - 1) / PGSIZE;

  // 3) 将这块内存从 free pool 中保留出来（放在 free region 的起始处）
  page_refcount = (uint32_t *)free_mem_start_addr;
  // 清零（注意：此处 page_refcount 处于尚未回收为 free list 的范围）
  memset(page_refcount, 0, refcount_pages * PGSIZE);

  // 4) 移动 free_mem_start_addr，避免后续 create_freepage_list 回收这部分页
  free_mem_start_addr += refcount_pages * PGSIZE;

  // 5) 现在继续调用 create_freepage_list(free_mem_start_addr, free_mem_end_addr);
```

3) helper：物理地址 ⇄ 索引

```c
static inline uint64_t pa_to_idx(uint64_t pa) {
  return (pa - DRAM_BASE) >> PGSHIFT; // PGSHIFT == 12
}
static inline uint64_t idx_to_pa(uint64_t idx) {
  return (idx << PGSHIFT) + DRAM_BASE;
}
```

4) 引用计数操作（使用 GCC 原子内建）：

```c
// 增加 ref 并返回增加后的值
uint32_t page_ref_inc_pa(uint64_t pa) {
  uint64_t idx = pa_to_idx(pa);
  return __sync_add_and_fetch(&page_refcount[idx], 1);
}

// 减少 ref 并返回减后值；如果减到 0，调用 free_page(pa) 释放物理页并返回 0
uint32_t page_ref_dec_pa(uint64_t pa) {
  uint64_t idx = pa_to_idx(pa);
  // fetch and sub，返回减前值
  uint32_t prev = __sync_fetch_and_sub(&page_refcount[idx], 1);
  if (prev == 1) {
    // 我们刚把计数从1减到0：现在安全释放物理页
    // 注意：free_page 需要 pa 在 freeable 范围内（我们的元数据页被保留，故不会误回收）
    free_page((void *)pa);
    return 0;
  }
  // prev >= 2, 现在的值是 prev - 1
  return prev - 1;
}

uint32_t page_ref_get_pa(uint64_t pa) {
  uint64_t idx = pa_to_idx(pa);
  // 读取是原子读取即可
  return __sync_add_and_fetch(&page_refcount[idx], 0);
}
```

说明/谨慎点：
- 使用 `__sync_fetch_and_sub` 能保证原子性并避免 double-free：只有当 `prev == 1` 时才释放，且 `prev` 是原子读到的先前值。如果另一个 CPU 同时 `inc`，原子操作顺序会保证不会错误释放。
- `free_page()` 在这里会把页面放回全局空闲链表；确保这个页面不是之前预留给元数据的页（我们在 `pmm_init` 中已预留）。

5) 在 fork（或 clone）中使用（伪代码）

- 当父进程页被复制到子进程（不复制物理内容，仅复用映射）时：
  - 在为子进程创建 PTE 时，复制父 PTE 的 PPN，但清除写位（PTE_W = 0），保留读/execute/用户位。
  - 调用 `page_ref_inc_pa(pa)`。
  - 更新父 PTE 也清除写位（保证写时触发 COW）。
  - flush TLB as needed。

伪代码：
```c
// 在 fork 处理每个映射页:
old_pa = PTE2PA(parent_pte);
child_pte = PA2PTE(old_pa) | (parent_pte & (PTE_R|PTE_X|PTE_U|...));
// ensure write bit cleared
child_pte &= ~PTE_W;
parent_pte &= ~PTE_W;
page_ref_inc_pa(old_pa);
```

6) 写时故障（COW 处理）
- 在 page fault handler 中检测是写保护触发且 PTE_V && (PTE_W == 0) && (PTE_R == 1)（即写入到只读共享页）：
  - pa = PTE2PA(pte);
  - if page_ref_get_pa(pa) > 1:
    - newpa = alloc_page(); if newpa==NULL -> OOM
    - memcpy(newpa, (void*)pa, PGSIZE);
    - page_ref_dec_pa(pa);    // 对旧页减一（如果变 0 则会释放）
    - 更新 PTE 为 PA2PTE(newpa) | 原权限并设置 PTE_W
    - flush_tlb for that VA
  - else (ref == 1):
    - 直接在 PTE 上设置 PTE_W（允许写），不需要复制
    - flush_tlb

这个流程保证了拷贝只在真的需要写入时发生（写时复制）。

内存开销估算
- 32-bit per page: 4 bytes * #pages。举例：512 MB 物理内存有 131072 个 4KB 页，开销 ~ 512 KB。可接受。若想更节省，可用 16-bit（最多 65535），或者自适应当页数 < 65535 时使用 uint16_t。

其它注意事项与增强建议
- Metadata 冗余保护：把 metadata 区域从 `alloc_page` 池中排除，或者给 metadata 页的 refcount 设为一个大值，防止误释放。
- 初始化内核与保留页的 refcount：
  - 在 `pmm_init()` 完成后，要把内核占用的物理页、以及之前为 refcount 自己占用的页的 refcount 初始化为 1，避免这些页被误回收。
  - 例如，对从 `KERN_BASE` 到 `_end` 的物理页做 `page_refcount[idx] = 1`。
- 原子类型：如果你的编译器支持 `<stdatomic.h>`，可以用 `atomic_uint_fast32_t`，更语义化。
- 错误/边界处理：确认 `pa` 在合法范围内并页对齐；`pa_to_idx()` 要做范围/对齐检查并 panic 或返回错误值以防 BUG。
- 清理与调试：给系统添加统计和调试接口（比如打印某个物理页的 refcount，或 dump refcount 数组片段），方便调试 COW 行为。
- 性能：原子操作会带来一定开销，但通常接受。若系统非常依赖吞吐，可以考虑按 N 页一个锁（粗粒度分段锁）或使用 cache-friendly 的分区计数器，但复杂度会显著增加。

示例整合步骤（我可以代劳）
1. 在 `pmm.h` 添加声明（`page_ref_init()`，`page_ref_inc_pa()`，`page_ref_dec_pa()` 等）。
2. 在 `pmm.c` 的 `pmm_init()` 中嵌入上面预留、初始化 `page_refcount` 的代码段，并对内核页进行初始化计数（1）。
3. 在 `fork`（或 `process` 复制）路径处更新 PTE 并调用 `page_ref_inc_pa()`。
4. 在写保护异常处理路径中实现上面的 COW 逻辑（在你的缺页/写保护中断处理代码里改写）。
5. 添加一些测试：简单 fork-run-write 的用户程序，验证父子共享读页、写时复制、计数变化与内存释放行为。

如果你希望我现在直接修改代码，我可以：
- 把 `page_refcount` 的分配/初始化实现进 `pmm_init()`（修改 `pmm.c`），
- 增加 `page_ref_inc_pa`/`page_ref_dec_pa`/`page_ref_get_pa` 函数实现（在 `pmm.c` 或新文件 `pmm_ref.c`），
- 并给出一个小的测试/使用示例（或把 fork/写保护处理处的伪代码改为实际代码）。

你要我现在把实现添加到仓库并提交，还是先只要更详细的代码片段或在特定位置的补丁？