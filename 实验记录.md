# 可是sem_init, sem_P, sem_V的参数都是值而不是地址，应该如何处理这个问题？
可以把参数当作“句柄”而不是直接操作用户态对象：

- `sem_new` 在内核创建信号量结构（含计数和等待队列），分配一个整型 ID，返回给用户。  
- `sem_P/sem_V` 的参数就是这个 ID，内核用它去信号量表里查找到对应的结构，再做 P/V 操作。  
- 用户态不需要也不应该直接操作信号量内存，所以传值没问题；关键是内核维护一张 ID→信号量的表，并在系统调用里根据 ID 做真正的修改。  
- 如果担心 ID 越界或被乱传，内核在查表前做校验，非法则返回错误码。

# **实现概要**
- **模型**：用户态只持有整型句柄，`sem_new` 在内核分配计数+等待队列；`sem_P`/`sem_V` 用句柄查表，`count`<=0 时阻塞、入队，`V` 时递增并唤醒队首。
- **系统假设**：单核、内核态不被抢占（调度只在显式 `schedule()` 处发生），因此无需额外原子/锁就能安全更新计数和队列。
- **关键坑点**
  - **EPC 回退**：阻塞前 `trapframe->epc -= 4`，让被唤醒后重新执行同一条 `ecall`，保证 P 语义正确。
  - **队列去重**：入等待队列前检查是否已存在当前进程，避免重复入队导致多次唤醒或队列膨胀。
  - **句柄校验**：`sem` 越界或未初始化返回错误，防止非法访问。

# 既然内核是在用户内核栈上跑的，为什么schedule后再回来就不再回到内核了？这是通过什么机制实现的，用户内核栈中的内容不需要清空而直接被覆盖吗？
**原因与机制**
- **切换到其他进程**：`schedule()` 选出新的 `current`，调用 `switch_to` 切换到该进程保存的内核上下文（内核栈指针、寄存器），随后通过 `return_to_user`（或等效路径）用新进程的 `trapframe->epc`/`sp` 返回用户态。旧进程的内核调用栈不会被继续执行。
- **为何不回到原内核栈帧**：上下文切换时保存的是“内核栈顶指针 + 寄存器快照”，恢复的是新进程的那一套，因此指令流直接落在新进程返回用户态的路径上，不会跳回旧的内核帧。
- **内核栈如何处理**：旧进程的内核栈内容仍然保留在它自己的内核栈里，不需要清空；等该进程下次陷入内核时，会从它的栈顶重新建立新的调用帧，旧帧可以被覆盖。不同进程各有独立的内核栈，互不干扰。